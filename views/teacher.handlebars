
<h3>{{firstNme}} {{lastName}}</h3>
<h4>{{email}}</h4> 
    <div class="teachercourse">
        <ul>
            {{#each coursesTaught}}
                <li><a href = "http://localhost:3000/teachercourse/{{_id}}"> {{courseCode}}</a></li>
            {{/each}}
        </ul>
    </div>
